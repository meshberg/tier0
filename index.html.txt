<!DOCTYPE html>

<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tier0 | 根香</title>
    <meta name="description" content="すべてが還る場所。すべてが生まれる場所。">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

```
    body {
        background-color: #000;
        color: #fff;
        font-family: "Hiragino Kaku Gothic ProN", "Noto Sans JP", sans-serif;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        overflow: hidden;
    }

    .container {
        text-align: center;
        padding: 2rem;
    }

    .title {
        font-size: 3rem;
        font-weight: 300;
        letter-spacing: 0.5em;
        margin-bottom: 2rem;
        opacity: 0;
        animation: fadeIn 3s ease-out 0.5s forwards;
    }

    .subtitle {
        font-size: 1.2rem;
        font-weight: 300;
        letter-spacing: 0.3em;
        color: #666;
        margin-bottom: 3rem;
        opacity: 0;
        animation: fadeIn 3s ease-out 1.5s forwards;
    }

    .subtitle ruby rt {
        font-size: 0.6em;
        color: #444;
    }

    .description {
        font-size: 0.9rem;
        font-weight: 300;
        line-height: 2.5;
        color: #555;
        margin-bottom: 4rem;
        opacity: 0;
        animation: fadeIn 3s ease-out 2.5s forwards;
    }

    .pulse-container {
        position: relative;
        height: 60px;
        display: flex;
        justify-content: center;
        align-items: center;
        margin-bottom: 4rem;
    }

    .pulse {
        width: 8px;
        height: 8px;
        background-color: #333;
        border-radius: 50%;
        animation: pulse 4s ease-in-out infinite;
    }

    .pulse-ring {
        position: absolute;
        width: 8px;
        height: 8px;
        border: 1px solid #222;
        border-radius: 50%;
        animation: ring 4s ease-out infinite;
    }

    .footer {
        font-size: 0.75rem;
        letter-spacing: 0.2em;
        color: #333;
        opacity: 0;
        animation: fadeIn 3s ease-out 3.5s forwards;
    }

    .footer a {
        color: #444;
        text-decoration: none;
        transition: color 0.3s ease;
    }

    .footer a:hover {
        color: #666;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @keyframes pulse {
        0%, 100% {
            opacity: 0.3;
            transform: scale(1);
        }
        50% {
            opacity: 0.8;
            transform: scale(1.2);
        }
    }

    @keyframes ring {
        0% {
            opacity: 0.5;
            transform: scale(1);
        }
        100% {
            opacity: 0;
            transform: scale(8);
        }
    }

    /* 背景のかすかな動き */
    .bg-gradient {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: radial-gradient(ellipse at center, #0a0a0a 0%, #000 70%);
        z-index: -1;
        animation: bgPulse 8s ease-in-out infinite;
    }

    @keyframes bgPulse {
        0%, 100% {
            opacity: 1;
        }
        50% {
            opacity: 0.8;
        }
    }

    /* レスポンシブ */
    @media (max-width: 600px) {
        .title {
            font-size: 2rem;
            letter-spacing: 0.3em;
        }

        .subtitle {
            font-size: 1rem;
        }

        .description {
            font-size: 0.8rem;
            padding: 0 1rem;
        }
    }
</style>
```

</head>
<body>
    <div class="bg-gradient"></div>

```
<div class="container">
    <h1 class="title">Tier0</h1>
    
    <p class="subtitle">
        <ruby>根香<rt>ねこう</rt></ruby>
    </p>
    
    <p class="description">
        すべてが還る場所<br>
        すべてが生まれる場所
    </p>
    
    <div class="pulse-container">
        <div class="pulse"></div>
        <div class="pulse-ring"></div>
    </div>
    
    <p class="footer">
        <a href="https://hightechyamananeko.jp">ハイテクヤマネコ</a>
    </p>
</div>
```

</body>
</html>